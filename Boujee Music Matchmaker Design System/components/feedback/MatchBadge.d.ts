/**
 * The results-page reveal: a twinkling disco-ball sphere in the brand's violet/coral tint, with the script match percentage glowing at its centre.
 */
export interface MatchBadgeProps {
  percent?: number;
  size?: number;
}
export function MatchBadge(props: MatchBadgeProps): JSX.Element;
