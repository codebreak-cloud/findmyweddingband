/**
 * The Stage 1 loading beat — a short run of keys playing while the match calculates. Music owns this moment; the ring owns progress.
 */
export interface LoadingBeatProps {
  label?: string;
}
export function LoadingBeat(props: LoadingBeatProps): JSX.Element;
