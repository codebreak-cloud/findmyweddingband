The row-level selection mark shared by every question type — songs, single-select answers, and future list selections.

```jsx
<HeartToggle selected={isSelected} onClick={toggle} />
```

Outline in quiet sand tone when unselected, fills solid ink on selection. This is the one selection mark in the system — never mix in checkmarks or ticks.
